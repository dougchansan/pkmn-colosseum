/**
 * @file musyx_candidate_80157280.c
 * @brief Candidate-only prefix view of the residual MusyX runtime.
 *
 * The implementation remains centralized in musyx_range_80157280.c while the
 * target text is partitioned around the exact hardware island. This candidate
 * wrapper is never linked as decompilation progress.
 */
#include "src/musyx/musyx_range_80157280.c"
