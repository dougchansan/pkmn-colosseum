# Confirmed-name leads needing signature/typing work

0 remaining (of the original proposals). These names are confirmed but the asm-wrapper `(void)` signature / untyped `lbl_` globals / a name collision block a clean bulk rename — each needs per-function decomp work (match the real prototype, type its globals). Wired leads have been moved to applied_symbols.txt.

| addr (fn_) | confirmed name | provenance | header proto in |
|---|---|---|---|
