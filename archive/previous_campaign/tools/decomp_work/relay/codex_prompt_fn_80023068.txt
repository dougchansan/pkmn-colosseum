Review this candidate C for fn_80023068 from a Pokemon Colosseum GCN decomp (CodeWarrior 1.3, -O4,p). The file tools/decomp_work/relay/kimi_fn_80023068_result.md contains GLM-5.1's analysis (signature, control flow, C body). Original asm is at tools/decomp_work/relay/fn_80023068_asm.s.

Please read both files, then return a review focused on:
1. Semantic correctness (any bugs or misinterpretations of the asm?)
2. ABI/type issues (signedness, parameter types, return types)
3. Variable ordering / stack-buffer sizing for CW -O4,p match (stack frame is 0x1C0 per stmw r26 at 0x1a8; that means 0x18 saved regs + ~0x1A8 locals)
4. Any match-sensitive areas to flag for the final Claude pass

Output ONLY: corrected/tightened C body + a short "Match-sensitive notes" section. Write your answer to tools/decomp_work/relay/codex_fn_80023068_review.md (overwrite if exists). Keep it under 200 lines. No preamble, no meta-commentary.
