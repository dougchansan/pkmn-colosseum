Review existing C for fn_80024F2C, fn_8002509C, fn_8002520C — all 0x170 bytes, currently 3.3% match each (C-active in #else, asm in #if 0). These look like sibling triplet functions in src/game/gs_title.c.

Task:
1. Read current C for all 3 functions in src/game/gs_title.c (grep for "fn_80024F2C - 0x", "fn_8002509C - 0x", "fn_8002520C - 0x" to find locations)
2. Extract target asm for fn_80024F2C using `python3 tools/_extract_asm.py fn_80024F2C` (save to tools/decomp_work/relay/fn_80024F2C_asm.s if needed). Also for 8002509C and 8002520C.
3. Compare the 3 asm files to identify what differs between siblings (probably a single constant or function pointer)
4. Identify the pattern bug in the current C that prevents matching
5. Produce corrected C for all 3 siblings that should byte-match

Write to tools/decomp_work/relay/codex_triplet_result.md:
- Section "Corrected C for fn_80024F2C" (complete body)
- Section "Diff vs fn_8002509C" (just the 1-2 lines that differ)
- Section "Diff vs fn_8002520C" (just the 1-2 lines that differ)
- Section "What was wrong" (short)
- Section "Required pragmas"

Reference: sibling functions in this file typically share structure with one constant/fn difference. See fn_80023DA8/DE4/E20 (error stub triplet, same structure, 3 different error codes). See fn_80020ADC (matched fix) for the `*(s8*)(r4+4)` byte-pointer pattern and `#pragma scheduling on` recipe. CW 1.3 with -O4,p.

Keep under 200 lines. No preamble.
