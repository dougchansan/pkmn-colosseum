Fix fn_80024DBC (0x170 bytes, 10.9% match, currently C-active #if 0 in src/game/gs_title.c, CW 1.3 -O4,p).

Read:
1. Current C in src/game/gs_title.c (grep "asm void fn_80024DBC")
2. Target asm at tools/decomp_work/relay/fn_80024DBC_asm.s

Per recent Codex analysis: fn_80024DBC/24F2C/2509C/2520C are a 4-sibling set where the only delta is the sign on two `*(s16*)(arg1 + 0x50/0x52)` assignments:
- 24DBC = (+,+)
- 24F2C = (+,-)
- 2509C = (-,+)
- 2520C = (-,-)

Current C is pseudo-registerized with pseudo r3/r4/r5/r27/...r31 + goto labels. Rewrite as clean typed C.

Known patterns that matched sibling fn_80024CDC (in same file, #pragma scheduling off + #pragma optimization_level 4):
- typed arguments `u8* arg0, u8* arg1`
- `s32 val; f32 pos[2];` stack slots
- switch/if on lbl_8047A370 for mode selection
- walk linked list via fn_8005DA18 → fn_8005D934 with bit7/bit6 flags
- final u16 stores to arg1+0x50 and arg1+0x52 with `(s16)(s32)(lbl_8047B8E0 + ((f32)r + lbl_8047A374))` form

Produce final C + rationale. Write to tools/decomp_work/relay/claude_fn_80024DBC_final.md with sections:
- ## Final C (ready to paste into #else)
- ## Rationale
- ## Expected match risk (high/medium/low)

Keep under 180 lines.
