Review existing C for fn_80020BA0 (Pokemon Colosseum GCN, GStitle_HandleInput, 0xFC bytes, CW 1.3 -O4,p). The current C is in src/game/gs_title.c in #else block after "fn_80020BA0 - 0x80020BA0" marker — the #if 1 is asm-active, #else has a C attempt that's 0% match.

Target asm: tools/decomp_work/relay/fn_80020BA0_asm.s

Tasks:
1. Read both files (src/game/gs_title.c around fn_80020BA0, and asm file)
2. Identify what's wrong with the C (wrong signature? wrong constants? missing/extra branches?)
3. Produce corrected C that should byte-match the target

Write result to tools/decomp_work/relay/codex_fn_80020BA0_result.md with:
- Corrected C body (complete, ready to paste in #else block)
- Short explanation of what was wrong
- Any pragmas needed (try `#pragma scheduling on` + `#pragma optimization_level 4` as default)

Keep under 100 lines. No preamble.
