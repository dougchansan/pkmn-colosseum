Decompile fn_80024308 (Pokemon Colosseum GCN, GStitle_AutodemoSetup, 0x130 bytes, CodeWarrior 1.3 -O4,p). Read asm: tools/decomp_work/relay/fn_80024308_asm.s.

Context: fn_80022EE4 was just matched 100% using `#pragma scheduling on` + local `extern s32 fn_80144574(void*, s32, s32, u16, s32);` (forcing a local s32-return extern prevents CW's extsh fold). See src/game/gs_title.c around line 3856 for the reference matched pattern.

This fn_80024308 has floating-point operations (lis r0, 0x4330; stw + lfd pattern is the int-to-float conversion idiom). Expect fn_800D3088 return converted to float somewhere.

Produce the FINAL C + pragmas ready to paste into src/game/gs_title.c (#if 0 / #else path). Write to tools/decomp_work/relay/claude_fn_80024308_final.md with:
- section "## Final C"
- section "## Rationale"
- section "## Expected match risk (high/medium/low)"

Keep under 150 lines. Be specific, no hedging.
