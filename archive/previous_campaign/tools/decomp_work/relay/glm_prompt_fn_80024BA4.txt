Fix fn_80024BA4 (0x138 bytes, 9.0% match, currently C-active #if 0 in src/game/gs_title.c, CodeWarrior 1.3 -O4,p).

Read:
1. Current C in src/game/gs_title.c (grep "asm void fn_80024BA4")
2. Target asm at tools/decomp_work/relay/fn_80024BA4_asm.s

The current C likely uses pseudo-registers (r3/r4/r5 etc variables, goto labels). Rewrite as clean typed C.

Sibling context: fn_80024A2C and fn_80024BA4 and fn_80024CDC are all "autodemo" functions in this module. Patterns to apply:
- Real variable names (u8* arg0, u8* arg1, etc.)
- Byte-pointer syntax *(s16*)(ptr+N) not ptr[N]
- #pragma scheduling off for this file's autodemo group (see fn_80024CDC as reference which matches with scheduling off)
- #pragma optimization_level 4

Write to tools/decomp_work/relay/glm_fn_80024BA4_result.md:
- Final C body
- Short control-flow notes
- Required pragmas

Keep under 180 lines.
