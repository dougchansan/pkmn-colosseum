Fix fn_80024A2C (0x178 bytes, 8.5% match, currently C-active #if 0 in src/game/gs_title.c, CodeWarrior 1.3 -O4,p).

Read both:
1. Current C in src/game/gs_title.c (grep for "asm void fn_80024A2C" to find the block)
2. Target asm: tools/decomp_work/relay/fn_80024A2C_asm.s

The current C is pseudo-registerized (uses pseudo r3/r4/r5/r27/...r31 variables and goto labels). This never matches CW codegen — it produces a different shape entirely.

Similar pattern was just fixed for fn_80020BA0 (matched 100%): refactor to clean typed C using real variable names, drop goto, use *(s16*)(ptr+N) byte-pointer syntax, add #pragma scheduling on (or off if it helps — try both).

Task: produce fully rewritten C body that should byte-match the asm. Write to tools/decomp_work/relay/codex_fn_80024A2C_result.md with:
- Final C body (complete, ready to paste)
- What was wrong
- Required pragmas

Keep under 200 lines. No preamble.
