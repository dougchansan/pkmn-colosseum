# fn_80023760 (gs_title.c, 140 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_title_fn_80023760.inc`

## File target
`src/game/gs_title.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_80023760(void* r3_ctx, void* r4_npc)` -- 2 args. Title screen party display with 6-iteration loop checking Pokemon availability. Uses fn_800141BC, fn_80014118, fn_8005D934. 140 lines, 0x1C0 stack.

**Externs needed**: fn_800141BC, fn_80014118, fn_8005D934, fn_8012A5B0, fn_80123FBC, fn_80082EA4

**Quirks**: stmw r26 saves 6 regs, `addi r29, r1, 0x1C` for local struct, clrlwi for u16 extraction.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_title.c` then `python3 tools/match_scan_file.py gs_title fn_80023760`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_title.c._

**Signature**: `s32 fn_80023760(void* r3_ctx, void* r4_npc)` -- 2 args. Title screen party display with 6-iteration loop checking Pokemon availability. Uses fn_800141BC, fn_80014118, fn_8005D934. 140 lines, 0x1C0 stack.

**Externs needed**: fn_800141BC, fn_80014118, fn_8005D934, fn_8012A5B0, fn_80123FBC, fn_80082EA4

**Quirks**: stmw r26 saves 6 regs, `addi r29, r1, 0x1C` for local struct, clrlwi for u16 extraction.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_title: fn_80023760 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
