# fn_80011A1C (gs_npc_interact.c, 83 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_80011A1C.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_80011A1C(void* r26_ctx, void* r27_npc, s32 r28_arg)` — 3 args. Dialog callback that creates a menu, waits for input, then conditionally sends a message and returns the result.

**Pseudocode**:
```c
s32 fn_80011A1C(void* ctx, void* npc, s32 arg) {
    u8 flags = ctx[0x16];  // lbz
    s32 r30;  // dialog result
    
    do {
        u8 byte_17 = ctx[0x17];  // lbz
        if (byte_17 != 0) {
            // Menu with 0xF6 (246) option
            r30 = fn_801026A4(0xF6, 0, npc, 0, arg, ctx, 1);  // crxor for variadic
        } else {
            // Menu with 0x4B (75) option
            r30 = fn_801026A4(0x4B, 0, npc, 0, arg, ctx, 1);
        }
        
        if (flags != 0) break;  // exit loop early
        
        if (r30 == 3) {
            // Result was "exit" — cleanup: unregister both resources
            if (fn_80102620(0x4B) != 0) {
                fn_80102568(0x4B, 0, 1);
            }
            if (fn_80102620(0xF6) != 0) {
                fn_80102568(0xF6, 0, 1);
            }
            // Send message 0xFB
            s32 local_8[2] = {0};
            r31 = fn_801026A4(0xFB, 0, &local_8, 0, 1, 0);
            fn_80102568(0xFB, 0, 1);
            
            if (r31 == 0x1207) {
                continue;  // special retry case
            }
            break;
        }
    } while (0);  // actually just one iteration unless r31 == 0x1207
    
    return r30;
}
```

**Externs needed**: `fn_801026A4`, `fn_80102620`, `fn_80102568`

**Quirks**:
- **Loop with conditional retry**: The `do-while` at the top loops if `flags == 0` and `r31 == 0x1207`, otherwise exits.
- **`crxor 6, 6, 6`**: Sets CR6[EQ]=0 before fn_801026A4 calls — MWCC convention for variadic argument count.
- **`stmw r26, 0x18(r1)` / `lmw`**: Saves r26-r31 (6 registers).
- Two different menu creation calls (0xF6 or 0x4B) depending on `ctx[0x17]`.
- `fn_80102620(ID)` checks if resource exists, `fn_80102568(ID, 0, 1)` unregisters it.
- **Stack var `s32 local_8[2]`**: stores `{0, something}` on stack for fn_801026A4 call.
- Use `#pragma push / peephole off / pragma pop`.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_80011A1C`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_80011A1C 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
