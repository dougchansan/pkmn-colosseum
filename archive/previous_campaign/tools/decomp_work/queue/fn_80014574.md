# fn_80014574 (gs_event_exec.c, 331 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_event_exec_fn_80014574.inc`

## File target
`src/game/gs_event_exec.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_80014574(void* r3_ctx)` -- 1 arg. Story battle callback with bit-flag checks and conditional rendering. Searches table at lbl_80266B58. 331 lines.

**Externs needed**: fn_80105624, fn_80205B8C, fn_8012640C, fn_80132A38, fn_800FA444, fn_800FA280, fn_800FB680, fn_80109220, lbl_80266B58, lbl_8047A2F8

**Quirks**: `clrlwi r0, r6, 28` extracts 4-bit field, table search pattern, `li r5, 0` default.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_event_exec.c` then `python3 tools/match_scan_file.py gs_event_exec fn_80014574`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_event_exec.c._

**Signature**: `void fn_80014574(void* r3_ctx)` -- 1 arg. Story battle callback with bit-flag checks and conditional rendering. Searches table at lbl_80266B58. 331 lines.

**Externs needed**: fn_80105624, fn_80205B8C, fn_8012640C, fn_80132A38, fn_800FA444, fn_800FA280, fn_800FB680, fn_80109220, lbl_80266B58, lbl_8047A2F8

**Quirks**: `clrlwi r0, r6, 28` extracts 4-bit field, table search pattern, `li r5, 0` default.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_event_exec: fn_80014574 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
