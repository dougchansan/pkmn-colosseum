# fn_80025730 (gs_title.c, 179 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_title_fn_80025730.inc`

## File target
`src/game/gs_title.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_80025730(void* r3_ctx)` -- 1 arg. Title screen initialization that creates a party display panel, renders Pokemon team data. Uses fn_8005D934, fn_8005DA18, fn_801040A0. 179 lines.

**Externs needed**: fn_8005D934, fn_8005DA18, fn_801040A0, fn_80103F74, fn_801081F8, fn_80113F48, lbl_80478DD8, lbl_80478DDC, lbl_80478DD4, lbl_80478DD0

**Quirks**: Multiple `li r0, 0x0` SDA global initializations, `lwz` SDA access pattern, bdnz loop for party iteration.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_title.c` then `python3 tools/match_scan_file.py gs_title fn_80025730`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_title.c._

**Signature**: `s32 fn_80025730(void* r3_ctx)` -- 1 arg. Title screen initialization that creates a party display panel, renders Pokemon team data. Uses fn_8005D934, fn_8005DA18, fn_801040A0. 179 lines.

**Externs needed**: fn_8005D934, fn_8005DA18, fn_801040A0, fn_80103F74, fn_801081F8, fn_80113F48, lbl_80478DD8, lbl_80478DDC, lbl_80478DD4, lbl_80478DD0

**Quirks**: Multiple `li r0, 0x0` SDA global initializations, `lwz` SDA access pattern, bdnz loop for party iteration.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_title: fn_80025730 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
