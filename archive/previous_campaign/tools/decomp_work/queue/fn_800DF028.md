# fn_800DF028 (gs_render.c, 65 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_render_fn_800DF028.inc`

## File target
`src/game/gs_render.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_800DF028(struct_RenderObj* r3_obj, struct_SomeData* r4_data)` — Takes a render object and some data pointer. Conditionally initializes a position/transform struct in the render object.

**Pseudocode**:
```c
void fn_800DF028(struct_RenderObj* obj, struct_SomeData* data) {
    void* dest_ptr = obj->unk_38;  // lwz r3, 0x38(r3)
    
    // Check if dest_ptr has the sentinel 0x102XXXX pattern (or specifically 0x0102FEFE)
    if ((u32)(dest_ptr >> 16) == 0x102) {  // addis r0, r3, 0x102 then cmplwi r0, 0xfefe
        // This means the pointer is invalid/stale — refresh it
        dest_ptr = obj->unk_8->unk_8->unk_58;   // triple dereference
        obj->unk_38 = dest_ptr;                    // store refreshed pointer
        dest_ptr = fn_801BBD84();                  // allocate new?
    } else {
        dest_ptr = obj->unk_8->unk_8->unk_58;     // just use existing
    }
    
    if (dest_ptr == NULL) return;  // safety check
    
    // Fill in struct fields from data
    dest_ptr->pos_x = fn_800EF548(data, 0);  // stw to offset 0
    dest_ptr->field_4 = fn_800EF4FC(data);     // sth to offset 4
    dest_ptr->field_6 = fn_800EF4F4(data);     // sth to offset 6
    dest_ptr->field_8 = fn_800EF3E0(data, 1);  // stw to offset 8
    
    // Sign check: returns u8, check if nonzero → store 1, else store 0
    u8 result = fn_800EF4E4(data);  // clrlwi r4, r3, 24
    s32 sign_flag = ((-result) | result) >> 31;  // neg r0, r4; or r0, r0, r4; srwi r0, r0, 31
    dest_ptr->field_C = sign_flag;
    
    // Zero-initialize floats
    f32 zero = lbl_8047CAC8;  // likely 0.0f
    dest_ptr->field_10 = zero;
    dest_ptr->field_14 = zero;
    
    // Finalize
    fn_800EF504(data);
    fn_800B8DF4();
    
    // Store pointer back
    obj->unk_8->unk_8->unk_58 = dest_ptr;
}
```

**Externs needed**: `fn_801BBD84`, `fn_800EF548`, `fn_800EF4FC`, `fn_800EF4F4`, `fn_800EF3E0`, `fn_800EF4E4`, `fn_800EF504`, `fn_800B8DF4`, `lbl_8047CAC8` (R2 constant, likely 0.0f)

**Quirks**:
- **Sentinel check**: `addis r0, r3, 0x102` then `cmplwi r0, 0xfefe` — this checks if the struct pointer at obj+0x38 has the pattern 0x0102FEFE (a sentinel value). This is a common pattern where invalid pointers are marked with a magic value.
- **Triple dereference**: `obj->unk_8->unk_8->unk_58` — chain of 3 pointer loads.
- **Sign bit extraction**: `neg r0, r4; or r0, r0, r4; srwi r0, r0, 31` = `((-x) | x) >> 31` which is 1 if x ≠ 0, 0 if x = 0.
- **Mixed field sizes**: offsets 0, 8 are `stw` (4 bytes), offsets 4, 6 are `sth` (2 bytes), offset 12 is `stw` (4 bytes), offsets 16, 20 are `stfs` (4 bytes float).
- Use `#pragma push / peephole off / pragma pop`.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_render.c` then `python3 tools/match_scan_file.py gs_render fn_800DF028`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_render.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_render: fn_800DF028 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
