# fn_800E93B8 (gs_material.c, 433 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_material_fn_800E93B8.inc`

## File target
`src/game/gs_material.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_800E93B8(void* r3, void* r4, void* r5_arg, s32 r6)` -- 4+ args. Material pool initialization that checks if pool base (lbl_80401490) exists. 433 lines, 0x50 stack.

**Externs needed**: fn_800D88DC, fn_800D888C, fn_800D6A00, fn_800D7820, fn_800D85D4, fn_800D67BC, fn_800D61E4, fn_800D5CB8, fn_800D59B8, lbl_80401490

**Quirks**: `lwz r0, 0x0(r31); cmplwi r0, 0x0` pool base check, two paths for new vs existing entries.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_material.c` then `python3 tools/match_scan_file.py gs_material fn_800E93B8`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_material.c._

**Signature**: `s32 fn_800E93B8(void* r3, void* r4, void* r5_arg, s32 r6)` -- 4+ args. Material pool initialization that checks if pool base (lbl_80401490) exists. 433 lines, 0x50 stack.

**Externs needed**: fn_800D88DC, fn_800D888C, fn_800D6A00, fn_800D7820, fn_800D85D4, fn_800D67BC, fn_800D61E4, fn_800D5CB8, fn_800D59B8, lbl_80401490

**Quirks**: `lwz r0, 0x0(r31); cmplwi r0, 0x0` pool base check, two paths for new vs existing entries.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_material: fn_800E93B8 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
