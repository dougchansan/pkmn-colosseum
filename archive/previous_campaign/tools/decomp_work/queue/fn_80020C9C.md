# fn_80020C9C (gs_title.c, 140 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_title_fn_80020C9C.inc`

## File target
`src/game/gs_title.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_80020C9C(void* r3_ctx)` -- 1 arg. Title screen function that loops checking if resource 0xAA (170) is available, then renders a title menu. Uses fn_800F0308 (vsync). 140 lines.

**Externs needed**: fn_800F0308, fn_80102620, fn_801026A4, fn_80102568

**Quirks**: Loop with `fn_800F0308` for vsync, `li r3, 0xAA` resource constant, `li r31, 0` / `li r30, 0x1` initial values.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_title.c` then `python3 tools/match_scan_file.py gs_title fn_80020C9C`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_title.c._

**Signature**: `s32 fn_80020C9C(void* r3_ctx)` -- 1 arg. Title screen function that loops checking if resource 0xAA (170) is available, then renders a title menu. Uses fn_800F0308 (vsync). 140 lines.

**Externs needed**: fn_800F0308, fn_80102620, fn_801026A4, fn_80102568

**Quirks**: Loop with `fn_800F0308` for vsync, `li r3, 0xAA` resource constant, `li r31, 0` / `li r30, 0x1` initial values.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_title: fn_80020C9C 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
