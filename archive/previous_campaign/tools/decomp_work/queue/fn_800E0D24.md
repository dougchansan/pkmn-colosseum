# fn_800E0D24 (gs_render.c, 48 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_render_fn_800E0D24.inc`

## File target
`src/game/gs_render.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_800E0D24(void)` — no args, no return. Initializes a global float array with precomputed values using a loop from 0 to 0xB4 (180 iterations).

**Pseudocode**:
```c
void fn_800E0D24(void) {
    // Initialize lbl_804011B8 (a float array of 181 entries)
    // f30 = lbl_8047CB30 (some constant, likely 1.0 or scaling factor)
    // f29 = lbl_8047CB38 (another constant, likely PI/180 or similar)
    // f31 = lbl_8047CB40 (double, likely 0x43300000_80000000 for int-to-float conversion)
    
    f32* dst = lbl_804011B8; // global float array base
    s32 i;
    for (i = 0; i < 0xB5; i++) {  // 181 entries (0..180)
        // Convert integer i to float using the FPR conversion trick:
        // xoris r0, r29, 0x8000  => flips sign bit of i
        // stw r31(0x43300000) + stw r0 to stack, then lfd+subs to convert
        // f0 = (f64)(i ^ 0x8000) - (f64)0x43308000...
        // Actually this is: f0 = (f32)i  via the double-int conversion trick
        f64 temp = (f64)(i | 0x80000000u) - dbl_43308000;  // int-to-float trick
        f32 val = f30 * (f32)temp;         // multiply by scale factor
        val = f29 * val;                    // multiply by another factor
        val = sinf(val);                    // fn_800CDBE0 is likely sinf()
        dst[i] = (f32)val;                  // frsp then stfs
    }
}
```

**Externs needed**: `fn_800CDBE0` (likely `sinf` or `cosf`), `lbl_804011B8` (global float array), `lbl_8047CB30`, `lbl_8047CB38`, `lbl_8047CB40`

**Quirks**:
- **Int-to-float conversion trick**: Uses the classic PPC double-conversion pattern:
  - `lis r31, 0x4330` then `xoris r0, r29, 0x8000` then `stw r31, stw r0, lfd, fsubs f31`
  - This is `(f64)i` conversion. `f31 = 0x4330000080000000` (double) and the XOR trick lets you convert s32→f64 via `stack_dbl = (0x43300000 << 32) | (i ^ 0x8000); result = stack_dbl - 0x4330_8000_0000_0000.0`.
- `frsp f0, f1` after the `sinf` call — forces result to single precision before storing.
- Loop count is 0xB5 (181), which is likely for a sine table covering 0..180 degrees.
- `f30` and `f29` are likely `PI/180.0` (or `1.0`) and `180.0/PI` — or vice versa, for degree↔radian conversion.
- Use `#pragma push / peephole off / pragma pop` and `#pragma fp_contract on` for the `fmuls` + `fmadds` chains.
- Need to declare `lbl_804011B8` as a global extern.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_render.c` then `python3 tools/match_scan_file.py gs_render fn_800E0D24`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_render.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_render: fn_800E0D24 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
