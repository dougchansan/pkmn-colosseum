# fn_800114A4 (gs_npc_interact.c, 166 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_800114A4.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_800114A4(void* r3, void* r4, void* r5, void* r6, void* r7, void* r8)` -- 6 args. Render setup with model creation and dialog dispatch. Gets render context, then dispatches based on u8 arg (clrlwi r0, r28, 24). Similar to fn_80010294 pattern.

**Externs needed**: fn_8020E204, fn_8020E1A4, fn_80205B8C, fn_8012640C, fn_800FA444, fn_800FB680, fn_80132A38, fn_802040E8, fn_801F4C14, fn_8011BEB4, fn_800FA280

**Quirks**: stmw r23 saves 9 regs, crxor for variadic calls, clrlwi for u8 extraction, multiple conditional render paths.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_800114A4`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

**Signature**: `void fn_800114A4(void* r3, void* r4, void* r5, void* r6, void* r7, void* r8)` -- 6 args. Render setup with model creation and dialog dispatch. Gets render context, then dispatches based on u8 arg (clrlwi r0, r28, 24). Similar to fn_80010294 pattern.

**Externs needed**: fn_8020E204, fn_8020E1A4, fn_80205B8C, fn_8012640C, fn_800FA444, fn_800FB680, fn_80132A38, fn_802040E8, fn_801F4C14, fn_8011BEB4, fn_800FA280

**Quirks**: stmw r23 saves 9 regs, crxor for variadic calls, clrlwi for u8 extraction, multiple conditional render paths.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_800114A4 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
