# fn_800117BC (gs_npc_interact.c, 137 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_800117BC.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_800117BC(void* r3_ctx, void* r4_npc, void* r5_arg)` — 3 args. Dialog/interaction loop that acquires a resource, presents choices via fn_801026A4, validates result, then waits for input/animation completion before releasing.

**Pseudocode**:
```c
s32 fn_800117BC(void* ctx, void* npc, void* arg) {
    void* r26_result;
    s32 r27;
    s32 r31_resource;

    // Select resource ID based on ctx[0x44]
    if (*(u8*)(ctx + 0x44) == 0) {
        r31 = 0x4C;
    } else {
        r31 = 0xF7;
    }

loop:
    // Acquire resource with timeout
    fn_801080CC(r31, 0x1E);                 // 0x1E = timeout

    // Present dialog (variadic, 8 args)
    r27 = fn_801026A4(r31, 0, npc, 0, arg, ctx, 1); // crxor 6,6,6
    if (r27 < 0) goto release;

    // Validate and process result
    void* r26_val = fn_80010128(ctx, (u16)r27);  // clrlwi r4, r27, 16
    if (r26_val == NULL) goto release;

    // Clean up resources: check and release 0x4C, 0xF9, 0xFA, 0xF7
    if ((u8)fn_80102620(0x4C) != 0) fn_80102568(0x4C, 0, 1);
    if ((u8)fn_80102620(0xF9) != 0) fn_80102568(0xF9, 0, 1);
    if ((u8)fn_80102620(0xFA) != 0) fn_80102568(0xFA, 0, 1);
    if ((u8)fn_80102620(0xF7) != 0) fn_80102568(0xF7, 0, 1);

    // Set active and wait for animation
    fn_80106394(r26_val, 1, 1);

    // Hardware input wait loop
    s32 input_received;
    do {
        do {
            u8 hw1 = (u8)fn_801F18DC(0);
            if (hw1 != 0) {
                u8 hw2 = (u8)fn_801F1700(0);
                if (hw2 == 1 && (u8)fn_80265924() == 1) {
                    input_received = 1;
                    break;
                }
            }
            u16 hw3 = (u16)fn_801EF634();
            if (hw3 == 1) {
                input_received = 1;
                break;
            }
            input_received = 0;
        } while (0);  // single-check, then falls through

        if (!input_received) {
            fn_800F0308();                     // vsync
        }

        // Check render state
        s32 r27_flag = fn_800F7AF0(1);
        s32 r3_flag = fn_800F7BC4(1);
        s32 flags = r3_flag & r27_flag;
        if ((flags & 0x300) == 0) {           // rlwinm r0, r0, 0, 22, 23
            continue;                          // animation not done
        }
        break;                                // animation done
    } while (!input_received);

    fn_80106080(1);                           // release render state
    goto loop;

release:
    fn_801080CC(r31, 0x20);                  // release with different timeout
    return r27;
}
```

**Externs**: `fn_801080CC`, `fn_801026A4`, `fn_80102620`, `fn_80102568`, `fn_80010128`, `fn_80106394`, `fn_801F18DC`, `fn_801F1700`, `fn_80265924`, `fn_801EF634`, `fn_800F0308`, `fn_800F7AF0`, `fn_800F7BC4`, `fn_80106080`

**Quirks**:
- **`crxor 6, 6, 6`** before `fn_801026A4` — MWCC variadic calling convention
- **stmw r26, 0x8(r1)** — saves r26-r31, 0x20 stack frame
- **`lbz r0, 0x44(r28)`** — resource ID selection: 0x4C (default) or 0xF7
- **`rlwinm r0, r0, 0, 22, 23`** — extracts bits 22-23: `(flags & 0x300)` for render state check
- **`clrlwi r4, r27, 16`** = `(u16)r27` — zero-extend dialog result
- **Hardware input polling loop**: `fn_801F18DC(0)`, `fn_801F1700(0)`, `fn_80265924()`, `fn_801EF634()`
- **`fn_800F0308()`** — vsync wait in render loop
- **`li r4, 0x1E`** = 30 frame timeout for acquire, **`li r4, 0x20`** = 32 for release
- **4 resource cleanup checks**: 0x4C, 0xF9, 0xFA, 0xF7 — each followed by conditional release
- Use `#pragma push / peephole off / pragma pop`

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_800117BC`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_800117BC 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
