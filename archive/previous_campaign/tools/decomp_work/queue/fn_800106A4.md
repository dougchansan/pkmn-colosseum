# fn_800106A4 (gs_npc_interact.c, 116 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_800106A4.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_800106A4(void* r3_ctx, void* r4_npc)` — 2 args. Switch-based NPC interaction that dispatches based on NPC ID, then optionally renders a model.

**Pseudocode**:
```c
s32 fn_800106A4(void* ctx, void* npc) {
    s32 r30 = 0;
    s16 npc_id = *(s16*)(npc + 6);
    
    switch (npc_id) {
        case 0xB4:  void* p = fn_80103FE4(); fn_80132A38(0x37, p); r30 = 0xCF; break;
        case 0xC1:  /* participant state check, 2 sub-cases */ r30 = 0x196 or 0x199; break;
        case 0x11CD: fn_80132A38(0x36, fn_80103FE4()); r30 = 0x196; break;
        case 0x11D8: /* field_4 check for 0xF7/0xF8, conditional calls */ break;
        default: r30 = 0; break;
    }
    
    if (r30 != 0) {
        s32 battle = fn_8001D834(ctx, npc);
        fn_800FB680(0, -2, battle, r30);
    }
}
```

**Externs needed**: `fn_80103FE4`, `fn_801040A0`, `fn_801040D0`, `fn_802037DC`, `fn_80132A38`, `fn_8001D834`, `fn_800FB680`

**Quirks**:
- **If/else chain** for NPC IDs: 0xB4, 0xC1, 0x11CD, 0x11D8. Not a jump table — uses `cmpwi/bge/beq` cascade.
- **`li r4, -0x2`** = passing -2 to fn_800FB680.
- **_nested state check in 0xC1**: `state == 0` → fn_80132A38(0x36, *(u32*)(p)); `state == 1` → r30 = 0x199.
- **0x11D8 case**: Checks `ctx[4]` for 0xF7/0xF8, calls fn_801040D0 and fn_802037DC.
- Use `#pragma push / peephole off / pragma pop`.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_800106A4`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_800106A4 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
