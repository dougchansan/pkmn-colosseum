# fn_800327FC (gs_npc_event.c, 464 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_event_fn_800327FC.inc`

## File target
`src/game/gs_npc_event.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_800327FC(void)` — no args, huge stack frame (0x2C0 bytes). Complex battle preparation function with two execution paths. 464 asm lines.

**Pseudocode (brief)**: Two major branches based on `lbl_8047A44A`:
1. **Active path**: Walks 10-entry pool (stride 0xCA), renders party data via fn_800F9E70, copies ~200 bytes of render fields to stack struct, calls fn_80032ED8 with display data (type=0x209). Clears lbl_803A3278[0..14].
2. **Inactive path**: Iterates 6 party slots, validates shadow Pokemon, computes team fitness scores via fn_80082EA4, finds best Pokemon index, stores to lbl_8047A448, builds display struct from lbl_803A3334 data, calls fn_80032ED8(type=0x209, ...).

Both paths converge: cleanup (fn_80102620/fn_80102510), timing loop (float accumulate until threshold), store lbl_8047A458 (0xD or 0xC).

**Key externs**: fn_800E0C04, fn_800F9E70, fn_80032ED8, fn_8012A5B0, fn_80123FBC, fn_8012640C, fn_80082EA4, fn_80102620, fn_80102510, fn_801C41C8, fn_801C40F0, fn_8010264C, fn_800F0308, fn_800D37CC, fn_800D3088, lbl_8047A44A, lbl_8047A430, lbl_8047A43C, lbl_8047A434, lbl_8047A448, lbl_8047A449, lbl_8047A458, lbl_804788B0, lbl_803A3334, lbl_803A3278, lbl_8047B9F8, lbl_8047BA00, lbl_8047BA08, lbl_8047BA10, lbl_8047BA18

**Quirks**: Massive 704-byte stack frame, psq_st/psq_l for f27-f31, stmw r14-r31, float timing loop with int-to-float conversion (xoris+stw+lfd+fsubs pattern), two completely different code paths, ~15 function calls, `stmw/lmw` saves 18 registers, `#pragma fp_contract on` for fdivs+fadds.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_event.c` then `python3 tools/match_scan_file.py gs_npc_event fn_800327FC`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_event.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_event: fn_800327FC 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
