# fn_800216E8 (gs_title.c, 133 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_title_fn_800216E8.inc`

## File target
`src/game/gs_title.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/run types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_800216E8(void* r3_ctx, s32 r4_arg)` — 2 args. Title screen roster/tournament lookup that copies a static data table to stack, searches entries, computes contest IDs, and dispatches rendering.

**Pseudocode**:
```c
void fn_800216E8(void* ctx, s32 arg) {
    // Copy 0x1F words from lbl_80266C7C (excluding first word) to stack buffer
    u32 buf[0x1E];  // at r1+0x8
    u32 *src = (u32*)(lbl_80266C7C - 4);  // adjusted for lwz/lwzu
    u32 *dst = (u32*)(buf - 1);  // adjusted for stw/stwu
    s32 r6 = ...; // from data load pattern
    s16 r9 = (s16)r6;  // extsh

    if (r9 <= 0) {
        // Negative/zero case: call with fallback value 0x4261
        fn_800F96E4(ctx, arg + 1, 0x4261);
    } else {
        // Search loop: walk entries in buf, each 0xC stride
        // Entry format: {u32 id, u32 field4, s16 offset4, s16 offset6}
        // Inner loop: walk linked data entries of stride 0x8
        s32 r10 = 0;
        s32 r8 = 0;
        // Find matching entry in buf[0..0x14)
        for (r10 = 0; r10 < 0x15; r10++) {
            u32 target_id = *(u32*)(buf + r10 * 3);  // stride 0xC → 3 words
            s32 match_idx = 0;
            void* entry_ptr = r5;  // linked data
            for (match_idx = 0; match_idx < r9; match_idx++, entry_ptr += 8) {
                if (target_id == *(u32*)(entry_ptr)) break;
            }
            if (match_idx < r9) break;
        }

        if (r10 >= 0x15) {
            // Not found — call with 0x4261
            fn_800F96E4(ctx, arg + 1, 0x4261);
        } else {
            // Found — compute contest IDs
            s32 r6_offset = r10 * 0xC;
            s32 r29_id = buf_entry_field4;
            s32 r0_shift = r8 << 3;  // slwi
            void* match_entry = r5 + r0_shift;
            s32 r11 = buf[r10 * 3 + 2];  // contest type
            s16 r31 = *(s16*)(match_entry + 4);  // lha
            s16 r30 = *(s16*)(match_entry + 6);  // lha
            s32 r7_data = some_pointer;

            if (r11 < 0) goto use_default;
            // Additional search for contest validity
            r10++;
            for (r10 = r10 + 1; r10 < 0x15; r10++) {
                // Check each subsequent entry for validity
                // ...
            }
use_default:
            fn_8011F4F0(r7_data);
            fn_80132A38(0x32, r7_data);
            fn_80132A38(0x2F, r31);
            fn_80132A38(0x30, r30);
            fn_800F96E4(ctx, arg + 1, r29_id);
        }
    }
}
```

**Externs needed**: `lbl_80266C7C` (SDA global), `fn_800F96E4`, `fn_8011F4F0`, `fn_80132A38`

**Quirks**:
- **Large stack frame**: `stwu r1, -0x120(r1)` — 0x120 bytes, uses `stmw r27, 0x10C(r1)`
- **Static data copy**: `lis/addi` to load `lbl_80266C7C`, then word-copy loop with `mtctr=0x1F`, `lwz/lwzu/stw/stwu`
- **Nested search loop**: outer loop over 0x15 entries (stride 0xC), inner loop over r9 entries (stride 0x8)
- **`extsh r9, r6`** — sign-extend halfword from loaded data
- **`lha r31, 0x4(r3)` / `lha r30, 0x6(r3)`** — signed halfword loads
- **`mulli r6, r10, 0xC`** — stride 0xC for outer entries
- **`slwi r0, r8, 3`** — stride 0x8 for inner entries
- **Fallback value `0x4261`** when entry not found
- Use `#pragma push / peephole off / pragma pop`

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_title.c` then `python3 tools/match_scan_file.py gs_title fn_800216E8`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_title.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_title: fn_800216E8 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
