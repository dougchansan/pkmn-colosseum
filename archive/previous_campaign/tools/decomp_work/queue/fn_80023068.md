# fn_80023068 (gs_title.c, 143 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_title_fn_80023068.inc`

## File target
`src/game/gs_title.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_80023068(void* r3_ctx, void* r4_npc)` -- 2 args. Title screen function that iterates party slots (fn_800141BC with arg=1), checks if Pokemon can battle. 143 lines, 0x1C0 stack.

**Externs needed**: fn_800141BC, fn_80123FBC, fn_8011FC74, fn_8011FC14, fn_8012A5B0, fn_8005D934

**Quirks**: `mr r3, r30; li r4, 0x1` party slot iteration, `cmpwi r29, 0x0` checks return value.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_title.c` then `python3 tools/match_scan_file.py gs_title fn_80023068`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_title.c._

**Signature**: `s32 fn_80023068(void* r3_ctx, void* r4_npc)` -- 2 args. Title screen function that iterates party slots (fn_800141BC with arg=1), checks if Pokemon can battle. 143 lines, 0x1C0 stack.

**Externs needed**: fn_800141BC, fn_80123FBC, fn_8011FC74, fn_8011FC14, fn_8012A5B0, fn_8005D934

**Quirks**: `mr r3, r30; li r4, 0x1` party slot iteration, `cmpwi r29, 0x0` checks return value.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_title: fn_80023068 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
