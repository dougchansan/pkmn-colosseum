# fn_800D7894 (gs_render.c, 48 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_render_fn_800D7894.inc`

## File target
`src/game/gs_render.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_800D7894(void)` — no args, returns a pointer (in r3) to a slot struct, or NULL (0) on failure. Walks a pool array to find an empty slot, initializes it.

**Pseudocode**:
```c
s32 fn_800D7894(void) {
    // SDA globals:
    //   lbl_8047AAB0(r13) = count/limit of pool slots
    //   lbl_8047AAAC(r13) = pointer to pool array base
    //   lbl_8047AAB4(r13) = next free slot index (round-robin)
    //   lbl_803144D0 = array of initial values for each slot
    
    s32 count = *(u32*)lbl_8047AAB0;   // max slots
    u8* ptr = *(u32*)lbl_8047AAAC;     // pool base
    
    if (count == 0) return 0;
    
    // Walk pool looking for empty slot (byte at offset 0 == 0 means free)
    do {
        if (ptr[0] == 0) {
            // Found a free slot — claim it
            ptr[0] = 1;  // mark as used
            
            // Set initial value from rotational index
            u32 idx = *(u32*)lbl_8047AAB4;
            s32* val_table = (s32*)lbl_803144D0;
            *(s32*)(ptr + 4) = val_table[idx];
            
            // Advance round-robin index (wraps at 8)
            idx = idx + 1;
            *(u32*)lbl_8047AAB4 = (idx < 8) ? idx : 0;
            
            // Zero out sub-fields at regular offsets (0x1C apart)
            ptr[0x08] = 0;
            ptr[0x24] = 0;
            ptr[0x40] = 0;
            ptr[0x5c] = 0;
            ptr[0x78] = 0;
            ptr[0x94] = 0;
            ptr[0xb0] = 0;
            ptr[0xcc] = 0;
            ptr[0xe8] = 0;
            ptr[0x104] = 0;
            ptr[0x120] = 0;
            ptr[0x13c] = 0;
            ptr[0x158] = 0;
            ptr[0x174] = 0;
            
            return (s32)ptr;   // return pointer to claimed slot
        }
        ptr += 0x190;  // stride = 0x190 = 400 bytes per slot
    } while (--count > 0);
    
    return 0;  // no free slot found
}
```

**Externs needed**: `lbl_8047AAB0`, `lbl_8047AAAC`, `lbl_8047AAB4`, `lbl_803144D0` (SDA/R2 globals as appropriate)

**Quirks**:
- **Pool allocator pattern**: Walks array with fixed stride (0x190 = 400 bytes per entry), looking for `byte[0] == 0` (free slot).
- Uses `mtctr/bdnz` loop — translate as `do { } while (--count)` or `for` loop.
- Round-robin index stored in SDA global `lbl_8047AAB4`, wraps at 8 (`cmplwi r0, 0x8; blt`).
- `mulli` not used for stride — instead `addi r3, r3, 0x190` increments pointer directly.
- Zeroes 13 sub-fields at offsets 0x08 through 0x174, each 0x1C (28) apart. This is a 14-entry array of sub-structs within each slot.
- SDA globals: r13+offset addressing. Use `extern u32 lbl_8047AAB0;` etc.
- The `cmplwi r0, 0x0` / `ble` at the start checks if count <= 0 (empty pool).

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_render.c` then `python3 tools/match_scan_file.py gs_render fn_800D7894`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_render.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_render: fn_800D7894 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
