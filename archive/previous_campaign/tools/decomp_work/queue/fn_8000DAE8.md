# fn_8000DAE8 (gs_npc_interact.c, 109 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_8000DAE8.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_8000DAE8(void* r3_ctx, void* r4_npc)` — 2 args. Time display NPC event: computes clock digits from a game-time index using multiply-high division, then renders the NPC.

**Pseudocode**:
```c
void fn_8000DAE8(void* ctx, void* npc) {
    s16 npc_id = *(s16*)(npc + 6);
    
    if (npc_id == 0x12AE || npc_id == 0x12AC) {
        // Time display: compute hours/minutes from fn_801040D0 result
        s32 idx = fn_801040D0(ctx, 0);
        u16* table = lbl_802E4B98;  // rodata digit lookup (stride 2)
        u8* dest = lbl_803A1B80;     // global display struct
        
        // Division by 60 using multiply-high: idx / 60
        s32 hours = idx / 60;
        s32 mins  = idx % 60;
        s32 h10   = hours / 6;     // hours / 10 (via mulhw)
        s32 h1    = hours - h10 * 10;  // hours % 10
        s32 m10   = mins / 10;     // via second mulhw
        s32 m1    = mins - m10 * 10;   // mins % 10
        
        dest[0] = table[m10 * 2];  // sth — minute tens digit
        dest[2] = table[h1 * 2];   // hour ones digit
        dest[4] = 0x3A;            // ':' separator
        dest[6] = table[m1 * 2];   // minute ones digit?  
        dest[8] = 0;               // null terminator
        dest[10] = table[h10 * 2]; // hour tens digit
        
        fn_80132A38(0x37, idx);
        s32 battle = fn_8001D834(ctx, npc);
        fn_800FB680(0, 0, battle, 0xCF);
    } else if (npc_id == 0x12B0) {
        fn_801040D0(ctx, 0);
        fn_80132A38(0x34, result);
        s32 battle = fn_8001D834(ctx, npc);
        fn_800FBB34(0, 0, npc[0x54], npc[0x56], battle, 0xDE);
    }
}
```

**Externs needed**: `fn_801040D0`, `fn_80132A38`, `fn_8001D834`, `fn_800FB680`, `fn_800FBB34`, `lbl_802E4B98` (digit lookup table), `lbl_803A1B80` (global display struct)

**Quirks**:
- **Multiply-high division**: `mulhw` with magic numbers 0x88890000-0x7777 and 0x66666667 for division by 60, 6, and 10.
- **`subi`**: Creates 0x88898888 - 0x7777 = division constant.
- **Digit lookup**: lbl_802E4B98 is a u16 array indexed by (digit*2) to get display character codes.
- **Struct at lbl_803A1B80**: 5 u16 fields (offsets 0,2,4,6,8,10) for clock display.
- **Two NPC IDs**: 0x12AC/0x12AE → time display path; 0x12B0 → simple render path.
- Use `#pragma push / peephole off / pragma pop`.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_8000DAE8`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_8000DAE8 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
