# fn_80010294 (gs_npc_interact.c, 130 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_80010294.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_80010294(void* r3_ctx, void* r4_npc)` — 2 args. Complex NPC render function that determines battle model positioning, checks participant state, and renders battle effects based on outcome.

**Pseudocode**:
```c
s32 fn_80010294(void* ctx, void* npc) {
    void* participant = fn_80103FE4();
    s32 battle_obj = fn_8001D834(ctx, npc);
    s32 model_handle = fn_80104530(ctx[4]);
    u16 model_dim = (u16)(fn_800FA444(0x197) >> 16);  // srwi + extsh pattern
    
    // Calculate Y offset: (npc_y - model_dim) / 2
    s16 npc_y = *(s16*)(npc + 0x54);
    s32 offset = ((npc_y - model_dim) >> 31) + (npc_y - model_dim);
    s16 r26 = (s16)(offset >> 1);  // extsh after srawi
    
    // Initial render call
    fn_800FB680(r26, 0, battle_obj, 0x197, 0);
    
    // Check participant state byte
    s8 state = (s8)participant[r30 * 0xC + 9];  // extsb from lbz
    if (state >= 0 && state < 4) {
        void* entry = participant + r27;  // r27 = state * 0xC
        if (entry[4] != 0) {
            void* obj = participant[0x40];  // lwz
            void* battle_ptr = fn_80205B8C(obj);
            if (battle_ptr != NULL) {
                s8 r6 = (s8)ctx[0x95];
                battle_result = (u16)fn_8012640C(battle_ptr, 0, 0x7F, r6);
                r0 = (u16)battle_result;
            } else {
                r0 = 0;
            }
            
            // Render based on battle result
            if (r0 == 0 || r0 == 0x164) {
                // Default render
                fn_800FB8C8(0, 0, r26, npc[0x56], battle_obj, 0x2BE1);
                fn_800FB8C8(0, 0, npc[0x54], npc[0x56], battle_obj, 0x2BE1);
            } else if (r0 == 0x165 || r0 == 0x166) {
                // Alt render
                fn_800FB8C8(0, 0, r26, npc[0x56], battle_obj, 0x2B6D);
                fn_800FB8C8(0, 0, npc[0x54], npc[0x56], battle_obj, 0x2B6D);
            } else {
                // Catch render
                fn_80132A38(0x34, entry[0xF]);
                fn_800FB8C8(0, 0, r26, npc[0x56], battle_obj, 0xD2);
                fn_80132A38(0x34, entry[0xE]);
                fn_800FB8C8(0, 0, npc[0x54], npc[0x56], battle_obj, 0xD2);
            }
        }
    }
}
```

**Externs needed**: `fn_80103FE4`, `fn_8001D834`, `fn_80104530`, `fn_800FA444`, `fn_800FB680`, `fn_80205B8C`, `fn_8012640C`, `fn_800FB8C8`, `fn_80132A38`

**Quirks**:
- **Multiple render functions**: `fn_800FB680` (5 args: r26, 0, model, 0x197) and `fn_800FB8C8` (8 args: 0, 0, r26/npc_x, npc_y, battle_obj, model_id).
- **srwi/extsh pattern**: `(s16)((s32)(u16)fn_800FA444(0x197) >> 16)` — takes upper 16 bits of model dimension.
- **Signed division by 2**: `subf; srwi; add; srawi; extsh` for `(npc_y - model_dim) / 2`.
- **`lbz r0, 0x9(r1)` + `extsb`**: Reads a signed byte from stack (extended to s32).
- **`mulli r27, r0, 0xC`**: Entry stride is 12 bytes per participant.
- **Model IDs**: 0x197, 0x2BE1, 0x2B6D, 0xD2 are render model constants.
- Use `#pragma push / peephole off / pragma pop`.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_80010294`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_80010294 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
