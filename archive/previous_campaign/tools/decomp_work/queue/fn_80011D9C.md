# fn_80011D9C (gs_npc_interact.c, 58 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_80011D9C.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_80011D9C(s32 r28_type, s32 r29_flag)` — takes two integer args. Determines a "duration" constant based on r28_type range, then calls fn_80103F74 and optionally fn_801081F8.

**Pseudocode**:
```c
void fn_80011D9C(s32 type, s32 flag) {
    s32 r30 = 0;
    void* r31 = fn_80104704();  // get some global/context pointer
    
    if (r31 == NULL) {
        return;  // early exit
    }
    
    // Determine r30 based on type value:
    // if (type == 0x45 || type == 0x49) => r30 = 0x538 (1336)
    // if (type == 0x47 || type == 0x48)        => r30 = 0x540 (1344)
    // else r30 = 0 (already initialized)
    s32 duration;
    if (type == 0x49) {
        duration = 0x538;  // 1336
    } else if (type >= 0x4B) {
        duration = 0;       // fallback
    } else if (type >= 0x47) {
        duration = 0x540;  // 1344
    } else if (type >= 0x45) {
        duration = 0x538;  // 1336
    } else {
        duration = 0;
    }
    
    if (flag != 0) {
        fn_80103F74(r31, duration, 1);    // enable with duration
        fn_801081F8(r31, duration, 0x2D); // set param with value 45
    } else {
        fn_80103F74(r31, duration, 0);    // disable with duration
    }
}
```

**Externs needed**: `fn_80104704`, `fn_80103F74`, `fn_801081F8`

**Quirks**:
- PPC branch pattern uses overlapping `cmpwi` + `bge` chains. MWCC generates this for if/else cascades on the same variable.
- The comparison tree from asm: `type >= 0x4B` → 0; `type == 0x49` → 0x538; `type >= 0x47` → 0x540; `type >= 0x45` → 0x538; else → 0.
- `fn_80104704()` takes no args — likely returns a global/singleton pointer.
- The `flag != 0` branch does two calls; the `flag == 0` branch does one.
- Use `#pragma push / peephole off / pragma pop` and match the if/else ordering exactly.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_80011D9C`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_80011D9C 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
