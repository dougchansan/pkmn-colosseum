# fn_800218BC (gs_title.c, 132 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_title_fn_800218BC.inc`

## File target
`src/game/gs_title.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_800218BC(s32 r28_id, s32* r29_result)` — 2 args. Table lookup for a Pokemon ID, validates battle fitness via party checks, returns success/failure.

**Pseudocode**:
```c
s32 fn_800218BC(s32 id, s32* result) {
    // Search lookup table (stride 0x8, entries at lbl_803A1B90)
    // Table format: {u16 key, u16 pad, u32 value} per entry
    s32 idx = 0;
    u8* table = lbl_803A1B90;
    s32 count = *(u32*)(table + 0x40);  // count at end of table header
    
    for (idx = 0; idx < count; idx++) {
        if (id == *(u16*)(table + idx * 8)) {
            break;
        }
    }
    
    if (idx >= count) {
        // Not found — show error message
        fn_80106D3C(2, 0x426A, 1, 0);  // error dialog
        fn_801069FC(1);
        return 1;  // error
    }
    
    // Found — validate party Pokemon fitness
    s32 r31 = 0;
    f32 f31 = lbl_8047B8A0;  // comparison threshold (likely 0.0f)
    
    for (r31 = 0; r31 < 6; r31++) {
        void* pokemon;
        fn_80014118(r31, &local_c, &local_8);  // get party slot info
        pokemon = local_c;
        
        if (fn_80123FBC(pokemon) != 0 && fn_8011FC74(pokemon) != 0) {
            f32 hp_ratio = fn_8011FC14(pokemon);  // get HP ratio
            if (hp_ratio > f31) {
                break;  // found valid Pokemon
            }
        }
    }
    
    if (r31 >= 6) {
        // No valid Pokemon found
        fn_80106D3C(2, 0x4261, 1, 0);
        fn_801069FC(1);
        return 1;  // error
    }
    
    // Try to enter battle with the found Pokemon
    s32 battle_result = fn_800141BC(id, 1);
    if (battle_result < 0) {
        goto cleanup;  // failed
    }
    
    fn_80014118(battle_result, &local_c, &local_8);
    s32 r30 = 0;
    
    if (fn_8011FC74(local_c) != 0) {
        f32 hp = fn_8011FC14(local_c);
        if (hp > lbl_8047B8A0) {
            r30 = 1;  // valid
        }
    }
    
    if (r30 == 0) {
        fn_80106D3C(2, 0x4261, 1, 0);
        fn_801069FC(1);
    }
    
cleanup:
    fn_80014198(battle_result);
    
    if (battle_result >= 0 && r30 != 0) {
        *result = 1;
        return 2;  // success
    }
    return 1;  // failure
}
```

**Externs needed**: `fn_80014118`, `fn_800141BC`, `fn_80014198`, `fn_80123FBC`, `fn_8011FC74`, `fn_8011FC14`, `fn_80106D3C`, `fn_801069FC`, `lbl_803A1B90` (rodata table), `lbl_8047B8A0` (R2 float constant)

**Quirks**:
- **Table search with bdnz loop**: Table at lbl_803A1B90 has stride 0x8, count at offset 0x40. Entries: {u16 key, ?, ?, u32 value} — 8 bytes per entry.
- **`psq_st f31 / psq_l f31`**: Paired-single save/restore for floating point.
- **Party validation loop**: Iterates 6 party slots (i < 6), checking `fn_80123FBC` (shadow Pokemon check) and `fn_8011FC74` (can battle check) and `fn_8011FC14` (HP ratio).
- **Return values**: Returns 1 for various error states, 2 for success (battle entered). `*result` is set to 1 on success.
- **f31 threshold**: `lbl_8047B8A0` is likely 0.0f — used to check HP ratio > 0 (i.e., Pokemon is alive).
- **`clrlwi r0, r3, 24`**: `(u8)result` — zero-extend byte check.
- **Error constants**: `0x426A` and `0x4261` are likely message IDs.
- Use `#pragma push / peephole off / pragma pop` and `#pragma fp_contract on` if needed.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_title.c` then `python3 tools/match_scan_file.py gs_title fn_800218BC`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_title.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_title: fn_800218BC 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
