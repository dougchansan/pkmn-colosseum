# fn_8000ED34 (gs_npc_interact.c, 428 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_8000ED34.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_8000ED34(void* r3, void* r4)` -- 2 args. Complex dialog callback with 4-iteration party member validation, shadow Pokemon checks, and conditional direction setting. 428 lines, 0x40 stack, stmw r23-r31.

**Externs needed**: fn_80105624, fn_801040D0, fn_80109220, fn_80123FBC, fn_80075FEC, fn_8011E8DC, fn_8001DA60

**Quirks**: 4-iteration loop checking party slots, conditional shadow Pokemon validation, two dispatch paths.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_8000ED34`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

**Signature**: `void fn_8000ED34(void* r3, void* r4)` -- 2 args. Complex dialog callback with 4-iteration party member validation, shadow Pokemon checks, and conditional direction setting. 428 lines, 0x40 stack, stmw r23-r31.

**Externs needed**: fn_80105624, fn_801040D0, fn_80109220, fn_80123FBC, fn_80075FEC, fn_8011E8DC, fn_8001DA60

**Quirks**: 4-iteration loop checking party slots, conditional shadow Pokemon validation, two dispatch paths.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_8000ED34 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
