# fn_80011700 (gs_npc_interact.c, 52 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_80011700.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_80011700(s32 r3_this)` — takes one struct pointer arg, returns 0. Pattern is: check if resource exists, if so register/unregister it.

**Pseudocode**:
```c
s32 fn_80011700(s32 r3_this) {
    s32 r31 = r3_this;  // save 'this' pointer
    
    // Check resource 0x4C (76)
    s32 ret = fn_80102620(0x4C);
    if ((u8)ret != 0) {
        fn_80102568(0x4C, 0, r31);  // arg1=0x4C, arg2=0, arg3=this
    }
    
    // Check resource 0xF9 (249)
    ret = fn_80102620(0xF9);
    if ((u8)ret != 0) {
        fn_80102568(0xF9, 0, r31);
    }
    
    // Check resource 0xFA (250)
    ret = fn_80102620(0xFA);
    if ((u8)ret != 0) {
        fn_80102568(0xFA, 0, r31);
    }
    
    // Check resource 0xF7 (247)
    ret = fn_80102620(0xF7);
    if ((u8)ret != 0) {
        fn_80102568(0xF7, 0, r31);
    }
    
    return 0;
}
```

**Externs needed**: `fn_80102620`, `fn_80102568`

**Quirks**:
- Very repetitive pattern: `fn_80102620(ID)` checks if something exists/enabled, returns a byte result. If nonzero, calls `fn_80102568(ID, 0, this)` to unregister/disable it.
- `clrlwi r0, r3, 24` = `(u8)ret` — zero-extends byte result.
- Four constant IDs checked: 0x4C, 0xF9, 0xFA, 0xF7. These are likely "flag index" or "resource slot" constants.
- Simple function — should match easily. Use `#pragma push / peephole off / pragma pop`.
- The return value is always 0 (`li r3, 0`).

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_80011700`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_80011700 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
