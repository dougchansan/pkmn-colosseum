# fn_800DD174 (gs_render.c, 72 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_render_fn_800DD174.inc`

## File target
`src/game/gs_render.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_800DD174(s32 r3_this)` — takes one arg. Linked-list removal function: finds a slot in the pool where `slot[0]==1 && slot[1]!=0`, removes it from a doubly-linked list, then cleans up.

**Pseudocode**:
```c
void fn_800DD174(s32 this_ptr) {
    s32 r31 = this_ptr;
    
    // Initialization call
    fn_801A4B00(0);
    
    // SDA globals:
    //   lbl_8047AAF0(r13) = max slot count
    //   lbl_8047AAEC(r13) = pool array base (stride 0x74)
    
    u8* pool_base = lbl_8047AAEC;
    s32 count = lbl_8047AAF0;
    s32 idx = 0;
    u8* cur = pool_base;
    
    // Search for active, non-empty slot: slot[0]==1 && slot[1]!=0
    if (count > 0) {
        do {
            if (cur[0] == 1 && cur[1] != 0) {
                goto found;
            }
            cur += 0x74;
            idx++;
        } while (idx < count);
    }
    idx = -1;  // not found
    
found:
    if (idx == -1) return;  // no active slot found
    
    // Remove from doubly-linked list:
    // Walk from idx+1 to end, unlinking active nodes
    s32 stride = idx * 0x74;
    u8* found_ptr = pool_base + stride;
    s32 next_idx = idx + 1;
    
    for (s32 j = next_idx; j < lbl_8047AAF0; j++) {
        u8* next_entry = pool_base + next_idx * 0x74;  // note: r4 = r7 * 0x74
        if (next_entry[0] == 1 && next_entry[1] != 0) {
            // Unlink: found_ptr->next = next_entry->next
            // But actually: next_entry->next->prev = found_ptr->prev
            s32 found_next = *(s32*)(found_ptr + 0x0C);
            s32 next_next = *(s32*)(next_entry + 0x0C);
            *(s32*)(found_next + 0x0C) = next_next;  // bypass: prev->next = next->next
            found_ptr = next_entry;  // move found_ptr forward
        }
        next_idx++;
    }
    
    // Terminate the found slot's list
    s32 found_next = *(s32*)(found_ptr + 0x0C);
    *(s32*)(found_next + 0x0C) = 0;  // null-terminate
    
    // Free the slot's sub-object
    s32 sub_obj = *(s32*)(pool_base + stride + 0x0C);
    fn_801A4D20(sub_obj);  // destroy/cleanup
    
    // Post-cleanup
    fn_801A4F54(r31);  // final cleanup with original arg
}
```

**Externs needed**: `fn_801A4B00`, `fn_801A4D20`, `fn_801A4F54`, `lbl_8047AAF0` (SDA global), `lbl_8047AAEC` (SDA global)

**Quirks**:
- **Doubly-linked list removal**: The pool entries are linked at offset 0x0C (next/prev pointers). This function finds the first active entry, then iterates through subsequent entries, relinking to bypass removed nodes.
- **Two-phase search**: First finds the index (`idx`) of a matching slot (byte[0]==1 && byte[1]!=0), then iterates from idx+1 to end for unlinking.
- **bdnz loop** for first search, then a `blt`-bounded loop for the unlinking pass.
- **Stride 0x74**: Same as fn_800DCFBC — entries are 116 bytes.
- **`mulli` for stride calculation**: `mulli r6, r3, 0x74` and `mulli r4, r7, 0x74`.
- **Linked list field at offset 0x0C**: `*(s32*)(entry + 0x0C)` is the next-pointer in the linked list.
- **Active check**: `slot[0] == 1` (active flag) AND `slot[1] != 0` (non-empty flag).
- Use `#pragma push / peephole off / pragma pop`.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_render.c` then `python3 tools/match_scan_file.py gs_render fn_800DD174`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_render.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_render: fn_800DD174 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
