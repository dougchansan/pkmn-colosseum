# fn_800DE09C (gs_render.c, 37 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_render_fn_800DE09C.inc`

## File target
`src/game/gs_render.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_800DE09C(f32 f1, f32 f2, f32 f3, f32 f4, f32 f5, f32 f6, f32 f7, f32 f8, ...)` — varargs with 8 float args + up to 8 integer variadic args.

**Pseudocode**:
```c
// This is a varargs thunk that packages all args into a struct and forwards to fn_800DE128
// The struct layout (on stack) appears to be:
//   offset 0x00: r3 (first int arg)
//   offset 0x04: r4
//   offset 0x08: r5
//   offset 0x0C: r6
//   offset 0x10: r7
//   offset 0x14: r8
//   offset 0x18: r9
//   offset 0x1C: r10
// The struct at r31 (sp+0x68) contains:
//   0x00: 0x300 (magic/model type)
//   0x04: va_list pointer (r11 = sp+0x88 = start of variadic args)
//   0x08: sp+0x08 (start of stored GPR args)
// Floats f1-f8 are stored conditionally if cr1 is not set (varargs convention).

void fn_800DE09C(/* varargs */) {
    // PPC varargs ABI: conditionally save float regs based on cr1
    // bne cr1 skips float save if no float variadics expected
    // Store r3-r10 at sp+0x08 through sp+0x24
    // Build a descriptor struct at r31:
    //   struct->type = 0x300;           // some model type
    //   struct->va_start = sp+0x88;     // pointer past last named arg slot
    //   struct->overflow = sp+0x08;    // saved GPR area
    fn_800DE128(/* constructed args struct */);
}
```

**Externs needed**: `fn_800DE128`

**Quirks**:
- This is a **varargs bridge/thunk** function. The `bne cr1` checks the FP arg descriptor from the caller.
- MWCC varargs convention: `cr1` indicates whether float regs contain args; `bne cr1` skips float saves when all args are in GPRs.
- The struct at sp+0x68 is a varargs descriptor: `{ s32 type; va_list* va; s32* overflow; }` where type=0x300.
- `lis r12, 0x300` is loading the constant 0x3000000 (0x300 << 16), but actually `lis r12, 0x300` = 0x00300000. Wait — `lis` shifts left 16, so `lis r12, 0x300` = 0x03000000. This is likely an enum/constant for the draw call type.
- The `mr r6, r31` overwrites r6 before it's saved to sp+0x10 — interesting reordering. r6 (arg 4) goes to sp+0x10, then r6 is replaced with the descriptor pointer for the inner call.
- **Implementation note**: This function will likely need `#include <stdarg.h>` and careful varargs handling. Consider implementing as a variadic wrapper that builds a `va_list`-like structure.
- `addi r11, r1, 0x88` is the va_start pointer (first variadic stack slot).
- `addi r0, r1, 0x8` is the saved-regs pointer (first GPR save slot).

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_render.c` then `python3 tools/match_scan_file.py gs_render fn_800DE09C`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_render.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_render: fn_800DE09C 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
