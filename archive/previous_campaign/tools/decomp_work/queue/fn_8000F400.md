# fn_8000F400 (gs_npc_interact.c, 228 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_8000F400.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_8000F400(void* r3, void* r4)` -- 2 args. Party validation with 5-iteration loop, shadow Pokemon checks, and battle model rendering. 228 lines, 0x50 stack.

**Externs needed**: fn_801040D0, fn_80109220, fn_80123FBC, fn_80075FEC, fn_8011E8DC, fn_8012640C, fn_800FBB34, fn_800FB680, fn_800FA280, fn_80132A38

**Quirks**: 5-iteration loop, model dim calculation (srwi+extsh), fn_800FBB34 with 8 args.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_8000F400`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

**Signature**: `void fn_8000F400(void* r3, void* r4)` -- 2 args. Party validation with 5-iteration loop, shadow Pokemon checks, and battle model rendering. 228 lines, 0x50 stack.

**Externs needed**: fn_801040D0, fn_80109220, fn_80123FBC, fn_80075FEC, fn_8011E8DC, fn_8012640C, fn_800FBB34, fn_800FB680, fn_800FA280, fn_80132A38

**Quirks**: 5-iteration loop, model dim calculation (srwi+extsh), fn_800FBB34 with 8 args.

## Codex Input

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_8000F400 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
