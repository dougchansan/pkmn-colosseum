# fn_8000F35C (gs_npc_interact.c, 44 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_8000F35C.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `void fn_8000F35C(some_struct* r3, some_struct* r4)` — r3 is some NPC/interaction state struct, r4 is saved for later use.

**Pseudocode**:
```c
void fn_8000F35C(struct_NpcInteract* r3_ctx, struct_NPCData* r4_npc) {
    // Check if ctx->field_0x4 == 0xF7 or 0xF8
    u32 flag = r3_ctx->unk_4;
    s32 r31_result = 1;  // default result
    if (flag == 0xF7 || flag == 0xF8) {
        // Try to acquire some resource
        u32 ret = fn_801040D0(2);
        if ((u8)ret != 0) {
            r31_result = 1;  // already set
        } else {
            r31_result = 0;  // resource not available
        }
    }

    // Jump table dispatch based on npc data offset 0x6 (halfword)
    s16 npc_id = r4_npc->field_0x6;  // lha
    u32 idx = (u32)(npc_id - 0x11CE);
    if (idx <= 9) {
        // switch on idx via jumptable_802E4C80
        switch (npc_id - 0x11CE) {
            case 0:  // npc_id 0x11CE - fall through
                // case 0 calls: fn_80109220(r4_npc, 1)
                break;
            case 1:  // npc_id 0x11CF
                // case 1 calls: fn_80109220(r4_npc, r31_result)
                break;
            // cases 2-9: likely similar patterns
        }
    }
}
```

**Externs needed**: `fn_801040D0`, `fn_80109220`, `jumptable_802E4C80`

**Quirks**:
- **Jump table**: Uses `jumptable_802E4C80` with `slwi r0, r0, 2; lwzx r0, r3, r0; mtctr r0; bctr`. The range is 0-9 (10 entries) for npc IDs 0x11CE-0x11D7.
- `lha r3, 0x6(r30)` — signed halfword load (s16).
- `subi r0, r3, 0x11CE` then `cmplwi r0, 0x9` — unsigned comparison, so this is `if ((u32)(id - 0x11CE) <= 9)`.
- Two jump table entries are visible: case 0 calls `fn_80109220(r4_npc, 1)`, case 1+ calls `fn_80109220(r4_npc, r31_result)`.
- The r31 variable holds a conditional result (1 or 0) based on `ctx->unk_4` checks (0xF7/0xF8 path vs default).
- `clrlwi r0, r3, 24` — zero-extends byte, i.e. `(u8)retval`.
- **Implementation**: Use a switch statement with cases 0-9 and a `default:` case. Need to find/read the jump table data at `jumptable_802E4C80` to determine all case targets.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_8000F35C`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_8000F35C 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
