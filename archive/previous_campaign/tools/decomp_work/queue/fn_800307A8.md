# fn_800307A8 (gs_npc_event.c, 83 asm lines)

## Status
- [x] GLM plan
- [x] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_event_fn_800307A8.inc`

## File target
`src/game/gs_npc_event.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match
- Already-matched functions in same file for regression check: fn_800308D4, fn_800318D8, fn_80030A44

## GLM Plan (planner)

### Inferred signature
```c
void fn_800307A8(u8* arg1, u8* arg2);
```
- arg1 (r3): context/state pointer (has byte at +0x8B for mode flag)
- arg2 (r4→r31): NPC data pointer (has s16 at +0x06 for NPC ID, u32 at +0x64 for table pointer, s16 at +0x54 for position Y, s16 at +0x56 for position Y2)

### Key externs needed
```c
extern void  fn_8012A5B0(void* partyData, s32 slot, s32 p3);  // already declared at line 99
extern void  fn_80132A38(s32 paramId, s32 value);               // already declared at line 100
extern void  fn_8012640C(s32, s32, s32, s32, u16, s32);       // already declared at line 1531
extern void  fn_800FBB34(s32, s32, s32, s32, u32, u16);        // already declared at line 2287
extern void  fn_80109220(void*, s32);                            // already declared at line 172
extern u8 lbl_803A2688[];                                       // already declared at line 142
extern u32 lbl_8047A424;  // SDA global (party state word at r13+offset)
extern u32 lbl_8047A420;  // SDA global (party state word at r13+offset)
extern u16 lbl_80266F68[]; // rodata table (NPC ID lookup table, stride 0x14, entries: u16 value, s16 key, ...14-byte stride)
```

### Pseudocode
```c
void fn_800307A8(u8* arg1, u8* arg2) {
    u32 table_ptr_raw;
    u16 match_value;
    u32 combined;
    void* result;
    u8 mode_byte;
    s16 npc_id;
    s16 table_key;
    u16 table_val;
    s32 battle_result;

    // Construct lookup key: OR the low 24 bits of arg2->dword_0x64 with arg1->byte_0x8B
    table_ptr_raw = *(u32*)(arg2 + 0x64);
    mode_byte = *(u8*)(arg1 + 0x8B);
    combined = (table_ptr_raw & ~0xFF) | mode_byte;  // clrrwi r4,r4,8 then or r30,r4,r0

    // Two-pass table search against lbl_80266F68 (stride 0x14)
    // Each entry: offset 0x00 = u16 value, offset 0x02 = s16 key
    match_value = 0;
    npc_id = *(s16*)(arg2 + 0x06);

    // First entry
    table_key = *(s16*)((u8*)lbl_80266F68 + 0x02);
    table_val = *(u16*)((u8*)lbl_80266F68 + 0x00);
    if (npc_id == table_key) { match_value = table_val; }

    // Second entry (offset 0x14)
    table_key = *(s16*)((u8*)lbl_80266F68 + 0x14 + 0x02);
    table_val = *(u16*)((u8*)lbl_80266F68 + 0x14 + 0x00);
    if (npc_id == table_key) { match_value = table_val; }

    // Dispatch on match_value
    result = NULL;
    if (match_value == 1) {
        result = (void*)fn_8012A5B0(lbl_803A2688, 3, (u16)lbl_8047A424);
    } else if (match_value == 2) {
        result = (void*)fn_8012A5B0(lbl_803A2688, 3, (u16)lbl_8047A420);
    }

    if (result != NULL) {
        battle_result = fn_8012640C(result, 0, 0x7f, 0, (s8)arg1[0x95], 0);
        fn_80132A38(0x2F, (u8)battle_result);
        fn_800FBB34(0, 0, *(s16*)(arg2 + 0x54), *(s16*)(arg2 + 0x56), combined, 0x4414);
        fn_80109220(arg2, 1);
    } else {
        fn_80109220(arg2, 0);
    }
}
```

### Quirks
- **Two-pass manual table search**: The asm does two explicit comparisons rather than a loop or switch — use two sequential `if` checks
- **SDA globals**: `lbl_8047A424` and `lbl_8047A420` are SDA-relative (r13+offset) — declare as `extern u32`
- **`clrrwi r4, r4, 8` + `or r30, r4, r0`**: This is `(dword & ~0xFF) | byte`, combining a masked dword with a byte
- **`(u16)lbl_8047A424`**: The `clrlwi r0, r0, 16` on an SDA global load means take low 16 bits
- **PRAGMA**: Use `#pragma push` / `#pragma peephole off` / `#pragma pop` around the function

### Expected patterns
- Table search with stride 0x14, two passes
- if/else dispatch on match_value (1 vs 2)
- fn_8012A5B0 returns void* (used as battle object pointer)
- fn_8012640C returns u16 battle result (clrlwi r3, r3, 16 means take low 16 bits)

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_event.c` then `python3 tools/match_scan_file.py gs_npc_event fn_800307A8`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_event.c._

- Final match: **64.3%** in worktree `wt-npc-event`
- Approach: converted the wrapper to typed C with the explicit two-entry `lbl_80266F68` search, then used local function-pointer casts for `fn_8012640C` / `fn_800FA280` so the file’s loose externs would not break compile.
- Verification: `python3 tools/compile_check.py src/game/gs_npc_event.c` passed; regression scan on nearby matched helpers stayed at 100%.
- Gotchas: the file already declares several helpers as `extern void fn_X(void)`, so local casts were safer than broad extern churn; this handler uses the `0x02` / `0x16` keys in `lbl_80266F68`, not the `0x06`, `0x08`, or `0x0E` variants used by sibling handlers.

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_event: fn_800307A8 0%->X.X% via <approach>`, move this file to completed/_

(fill in)
