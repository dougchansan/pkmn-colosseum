# fn_800109A0 (gs_npc_interact.c, 116 asm lines)

## Status
- [x] GLM plan
- [ ] Codex implementation
- [ ] Claude review+merge

## Target asm
See `src/game/gs_npc_interact_fn_800109A0.inc`

## File target
`src/game/gs_npc_interact.c`

## Baseline
- Current: asm wrapper at 100% (target identical bytes, but wrapper, not real C)
- Target: ≥60% real C match

## GLM Plan (planner)
_GLM-5.1: read the .inc, identify args/return types, describe pseudocode, list externs needed. Note any quirks (switch, table-search, bit-ops, FP math, list walks, etc.)_

**Signature**: `s32 fn_800109A0(void* r3_ctx)` — 1 arg. Initializes Pokemon party display, copies party data, sets availability flags, and resolves a dialog result.

**Pseudocode**:
```c
s32 fn_800109A0(void* ctx) {
    s8 flag = (s8)ctx[1];  // extsb
    
    if (flag == 0) {
        // Copy party data to slot 0x48
        void* src = fn_80103FFC(0x48);
        if (src != NULL) {
            void* party = fn_801040D0(ctx, 0);
            memcpy(src, party, 0x48);
        }
        // Reset state
        void* state = fn_801040A0();
        state[0] = 0;   // clear byte
        state[1] = 0;   // clear byte
        state[2] = -1;  // set to -1 (0xFF)
    }
    
    void* participant = fn_80103FE4();
    
    // Set display flags based on party member availability
    if ((s8)ctx[1] == 5) {
        // All 4 slots shown
        for (int i = 0; i < 4; i++) {
            fn_8005D8F8(*(u16*)(lbl_80478850 + i * 2), 1);
        }
    } else {
        // Conditional: show only if participant has Pokemon in slot
        void* entry = participant;
        for (int i = 0; i < 4; i++) {
            if (*(u32*)(entry + 4) != 0) {
                fn_8005D8F8(*(u16*)(lbl_80478850 + i * 2), 1);
            } else {
                fn_8005D8F8(*(u16*)(lbl_80478850 + i * 2), 0);
            }
            entry += 0xC;  // stride 12
        }
    }
    
    // Resolve dialog result
    s32 result = fn_801022B8(ctx[4]);  // get dialog ID
    s32 value;
    switch (result) {
        case 0xC4: value = 0; break;   // stw r0, 0x80
        case 0xC5: value = 1; break;
        case 0xC6: value = 2; break;
        case 0xC8: value = 3; break;    // 0xC7 maps here too
        default:   value = -1; break;
    }
    ctx->unk_80 = value;
    
    return 0;
}
```

**Externs needed**: `fn_80103FFC`, `fn_801040D0`, `fn_801040A0`, `fn_80103FE4`, `fn_8005D8F8`, `fn_801022B8`, `lbl_80478850` (SDA global)

**Quirks**:
- **`extsb r0, r0`**: Sign-extends byte at ctx[1] for comparison.
- **`memcpy(dst, src, 0x48)`**: Copies 72 bytes of party data. Uses `li r5, 0x48` for size.
- **Two display loops**: If flag==5, shows all 4 slots unconditionally; otherwise checks `entry[4] != 0`.
- **`la r30, lbl_80478850(r13)`**: SDA-relative array of u16 display IDs.
- **Stride 0xC per party entry** in conditional loop.
- **Result switch**: 0xC4→0, 0xC5→1, 0xC6→2, 0xC8→3 (0xC7 also maps to 3 via `bge`), else → -1.
- Use `#pragma push / peephole off / pragma pop`.

## Codex Input (implementer, iteration 1)
_Codex: implement the C based on GLM's plan. Wrap with `#pragma push / peephole off / pragma pop`. Run `python3 tools/compile_check.py src/game/gs_npc_interact.c` then `python3 tools/match_scan_file.py gs_npc_interact fn_800109A0`. If <60%, try the pragma variants (scheduling off), reorder locals, or use pointer-walk patterns. Commit when ≥60% OR after 5 attempts. Regression-check 3 already-matched functions in gs_npc_interact.c._

(fill in)

## Claude Review (merger)
_Claude: verify compile + match, check no regression, commit to master with message `gs_npc_interact: fn_800109A0 0%->X.X% via <approach>`, move this file to completed/._

(fill in)
