# Campaign 2026-06-27: Real-C Completion + Model Flywheel

## Rules

- No `.inc` wins, no asm wrappers, no inline asm.
- Real C must stay active; logged equivalents are hard-skip until a new lever exists.
- Pointer-arithmetic/register-dump C is intermediate only, not finished decomp quality.
- Parent/orchestrator re-measures every claimed win before trusting it.

## Active Plan

- [x] Clean old branches/worktrees and preserve dirty state in stash.
- [x] Restart fleet on a clean campaign branch.
- [x] Use 3090 SeedCoder lane with richer prompts: current C, residual classifier, KG lever context.
- [x] Save 3090 candidate/score traces to `build/seed_training/attempts.jsonl`.
- [x] Save Codex/GLM prompts/log summaries to `build/agent_training/`.
- [x] Expose dashboard on Tailscale: `http://100.124.217.13:8770/`.
- [x] Poll Windows permuter status into dashboard.
- [x] Add live-measured real-C queue builder to avoid stale `report.json` pins.
- [x] Rebuild live queue and restart fleet from measured non-100 real-C targets.
- [x] Push campaign branch to GitHub.
- [ ] Verify all newly banked `build/band_wins/*.json` entries are real C and still measure 100%.
- [ ] Import legitimate wins; reject `.inc`/asm-wrapper/pointer-dump fraud.
- [x] First-pass source audit for active asm wrappers.
- [x] First-pass source audit for raw pointer arithmetic/register-dump functions.
- [x] First-pass function-count denominator audit.
- [x] Add reusable real-C debt audit script.
- [x] Fix `progress2.py --measure`/`measure_cache.py` objdiff-cli resolution and rerun rigorous three-axis join.
- [x] Review permuter import win claims in parent gate.
- [x] Tighten CI fraud gate: reject newly added `.inc` includes, inline asm, and raw pointer-offset C.
- [x] Fix win JSON metadata handling for double-underscore SDK symbols.
- [x] Parent-verify and apply current Codex/OS wins.
- [ ] Deep audit source for active asm wrappers by TU and function name.
- [ ] Deep audit source for raw pointer arithmetic/register-dump functions by function name.
- [ ] Build rename audit for `fn_XXXXXXXX` symbols; do not apply broad renames until reviewed.
- [ ] Review whether `file_fnXXXXXXXX` temporary names are safe with config/symbol/linker constraints.
- [ ] Build roadmap from current axes: function match, byte-exact real C, fully linked, readability.

## Current Operational Notes

- Branch: `campaign-3090-training-closure`.
- Dashboard: `http://100.124.217.13:8770/`.
- 3090 model server: `http://192.168.50.101:8780/`, currently `seedcoder8b-cw-v3` bf16.
- Windows permuter status is written to `build/permuter_status.json`.
- Dashboard and permuter poller are running inside tmux panes (`sonnet` and `opus`) because detached `nohup` children launched from this execution environment exit.
- Use `python3 tools/decomp_work/live_real_c_queue.py --max-files 24` before fleet launch.
- Then regenerate fleet queue with `BUCKET_MIN_FILES=24 python3 tools/decomp_work/gen_bucket_queue.py`.

## Initial Audit Results

- Source scan: `REAL_C=7138`, `ASM-wrapper=776`, `STUB=427`, `total=8341`; C-converted scan axis is `85.58%`.
- Tracked source files: 208. All `src/**/*.c` files are tracked now; there are 0 untracked source `.c` files inflating the local source count.
- Current object-level progress command: `2742/4473 functions = 61.30%`, `284112/759144 bytes = 37.43%`.
- Active `.inc` include lines: 2505 across 54 tracked source files.
- Inline/asm token hits: 2662 across 89 tracked source files.
- Raw pointer-arithmetic pattern hits: 21095 across 141 tracked source files.
- Register-local pattern hits: 80864 across 116 tracked source files.
- Top `.inc` concentration: `gs_render.c` 258, `scene_init.c` 258, `gs_field_world.c` 245, `gs_material.c` 138, `battle_logic.c` 130, `people_field.c` 113, `gs_thread.c` 107, `effect_util.c` 102, `ui_core.c` 99.
- Top pointer/register debt concentration: `colosseum_battle.c`, `gs_field_world.c`, `battle_logic.c`, `gba_misc.c`, `ui_core.c`, `GXInit.c`, `menu_middle.c`, `trainer.c`, `gba_conv.c`.
- `progress2.py --measure` now works through the shared `measure_cache.py` objdiff path resolution.

## Rigorous Progress Snapshot

Command: `python3 tools/decomp_work/progress2.py --measure --list-equiv build/equiv_pool.tsv`

- Source scan: `REAL_C=7138`, `ASM-wrapper=776`, `STUB=427`, `total=8341`.
- Objdiff universe: `4473` function symbols.
- ROM-reproducible/object match: `2742/4473 = 61.30%`.
- C-converted in objdiff universe: `3715/4473 = 83.05%`.
- Byte-exact real C: `2369/4473 = 52.96%`.
- Real-C near/equivalent pool: `1346` functions, written to `build/equiv_pool.tsv`.
- ROM-only asm/split contribution: `352` functions. This is the gap between honest byte-exact C and the public ROM-reproducible metric.

## Debt Audit Snapshot

Command: `python3 tools/decomp_work/audit_real_c_debt.py --top 12`

- Output: `build/real_c_debt_audit.json` and `build/real_c_debt_audit.md`.
- Tracked source files: `208`.
- Active asm-wrapper functions: `776` across `61` files.
- Stub functions: `427`.
- `fn_`-named definitions: `6483`; `fn_` references: `15079`.
- `.inc` include lines: `2505` across `54` files.
- Raw pointer-offset lines: `19213` across `128` files.
- Register-named lines: `101092` across `119` files.
- Top asm-wrapper TUs: `GXInit.c` 122, `ui_core.c` 64, `gs_field_world.c` 63, `scene_init.c` 47, `people.c` 40, `effect_visual.c` 35, `gs_thread.c` 35, `gs_worldmap.c` 35.
- Top pointer-offset TUs: `gs_field_world.c`, `battle_logic.c`, `ui_core.c`, `gs_material.c`, `menu_carde_matrix.c`, `menu_middle.c`, `gs_thread.c`, `gba_conv.c`.

## Permuter Win Review

- Reviewed `pl_permuter_import_safe` and `pl_permuter_import_fast`.
- Parent dry-run re-verified three byte-exact real-C bodies:
  - `src/game/colosseum_event.c::fn_8020C0C0`
  - `src/game/colosseum_event.c::fn_8020C0E4`
  - `src/game/people/people.c::fn_8018FC50`
- All three are already active real C in canon, so they are redundant/no-op wins, not new progress.
- Remaining permuter imports failed parent verification because their TUs still require missing local `.inc` scaffolding or have compile-breaking prototype/body issues after splice. They are not mergeable wins.
- Current guidance: keep Windows permuter at one focused target/worker. The one-at-a-time approach is easier to parent-gate, produces cleaner lever attribution, and avoids multi-target stale-scratch contamination.

## Verified Wins Applied

- `src/dolphin/os/OSTime.c`: active typed C now compiles 5/5 byte-exact in the TU:
  - `__OSGetSystemTime`
  - `fn_800A27FC`
  - `fn_800A2998`
  - Existing asm primitives `OSGetTime`/`OSGetTick` remain exact.
- `src/dolphin/os/OSInterrupt.c`:
  - `__OSMaskInterrupts` `86.18% -> 100%`
  - `__OSUnmaskInterrupts` `86.18% -> 100%`
- `OSInterrupt.c` win required the `symbols.build.txt` reloc name `SetInterruptMask` for `0x8009E02C`; this is evidence-backed by `config/GC6E01/symbols.build.txt`.
- No `.inc` files, asm wrappers, or inline asm were added for these wins.

## Roadmap

- **Axis 1: C-converted/readable C**: convert 776 active asm wrappers and 427 stubs to real C. First buckets should be stub-heavy files (`battle_waza.c`, `battle_scene.c`, `gs_model.c`, `gs_field_colquery.c`, `gs_colsys.c`) and asm-heavy but tractable files after excluding known walls.
- **Axis 2: ROM-reproducible/public match**: move real-C near misses from the `build/equiv_pool.tsv` top into 100% with known levers; do not use asm wrappers or `.inc` includes to move this number.
- **Axis 3: byte-exact real C**: current strict count is `2369/4473 = 52.96%`; prioritize near-100 real-C functions because they raise the honest PC-port axis fastest.
- **Readability**: attack raw pointer-offset hot spots by recovering structs/fields per TU, not by broad mechanical rewrites. Track raw pointer C as debt until typed.
- **Naming**: do not broadly rename `fn_XXXXXXXX` yet. First produce a rename audit. A mechanical `file_fnXXXXXXXX` alias scheme is traceable but creates huge churn and does not improve matching or readability unless tied to symbol/config constraints.
- **Model flywheel**: keep logging SeedCoder attempts plus Codex/GLM `TRAINING_SUMMARY` records. Mine successful levers from KG and rejected candidates as negative examples before the next prompt-match fine-tune.

## Review Questions To Answer

- How many active asm wrappers remain, and which TUs concentrate them?
- How many real-C functions are low-readability pointer/register dumps?
- Which `.inc`/wrapper targets are currently counted as matching by objdiff but should not count as decompiled?
- Why does local function count differ from decomp.dev/report count?
- Which fully-linked units are one or two real-C wins away after stale/equivalent targets are removed?
- Should broad `fn_XXXXXXXX -> file_fn_XXXXXXXX` renaming be done by symbol aliases only, or deferred until evidence-backed names exist?
